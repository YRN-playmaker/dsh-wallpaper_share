{
  "name": "dwp-web",
  "version": "0.1.0",
  "description": "DWP web assembly layer: mount/Handle, playback clock, Canvas2D fallback executor. Only layer allowed to read wall clock.",
  "license": "MIT",
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "dwp-core": "workspace:*",
    "dwp-gl": "workspace:*"
  },
  "scripts": {
    "test": "node --test --experimental-test-isolation=none \"test/*.test.ts\""
  }
}
